/node_modules
/.next/
/out/
/build
.DS_Store
*.pem
npm-debug.log*
.env*.local
.vercel
*.tsbuildinfo
next-env.d.ts

.vercel

# Agent worktrees — scratch clones with their own node_modules.
.claude/
# ...except the agent definitions, which are project tooling and should travel
# with the repo. launch.json stays out: it holds this machine's absolute paths.
!.claude/agents/
!.claude/agents/**

.env*
